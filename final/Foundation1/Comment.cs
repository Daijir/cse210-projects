public class Comment
{
    // tracking commenter's name and the text of the comment.
    public string _commenter;
    public string _text;

    public Comment(string commenter, string text)
    {
        _commenter = commenter;
        _text = text;
    }
}

